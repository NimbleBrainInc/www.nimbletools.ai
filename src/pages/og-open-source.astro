---
import nimbleToolsLogo from "../assets/images/nimbletools-logo-n-transparent.png";
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NimbleTools Open Source Announcement</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 100vw;
            height: 100vh;
            min-width: 1920px;
            min-height: 1080px;
            background: radial-gradient(ellipse at center, #0f0f0f 0%, #000000 70%, #000000 100%);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            color: white;
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        /* Mysterious grid background */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                linear-gradient(rgba(0, 255, 136, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 255, 136, 0.03) 1px, transparent 1px);
            background-size: 60px 60px;
            opacity: 0.7;
            animation: pulse 4s ease-in-out infinite;
        }

        /* Glowing orb effect */
        body::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(0, 255, 136, 0.1) 0%, transparent 70%);
            border-radius: 50%;
            animation: glow 6s ease-in-out infinite alternate;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 0.7; }
        }

        @keyframes glow {
            0% { opacity: 0.1; transform: translate(-50%, -50%) scale(1); }
            100% { opacity: 0.3; transform: translate(-50%, -50%) scale(1.2); }
        }

        .container {
            position: relative;
            z-index: 1;
            padding: 80px;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 100vh;
        }

        .header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 0;
        }

        .logo {
            width: 80px;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            background: transparent !important;
            border: none;
            outline: none;
        }

        .brand {
            font-size: 48px;
            font-weight: 600;
            color: white;
        }

        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 40px;
            text-align: center;
        }

        .announcement-badge {
            display: inline-flex;
            align-items: center;
            gap: 16px;
            background: rgba(0, 255, 136, 0.08);
            border: 1px solid rgba(0, 255, 136, 0.2);
            padding: 20px 40px;
            border-radius: 100px;
            font-size: 28px;
            font-weight: 500;
            width: fit-content;
            color: rgba(0, 255, 136, 0.9);
            animation: flicker 3s ease-in-out infinite;
        }

        .eye {
            font-size: 32px;
            animation: blink 4s ease-in-out infinite;
        }

        @keyframes flicker {
            0%, 100% { opacity: 0.7; }
            50% { opacity: 1; }
        }

        @keyframes blink {
            0%, 90%, 100% { opacity: 1; }
            95% { opacity: 0.3; }
        }

        .title {
            font-size: 144px;
            font-weight: 300;
            line-height: 1.1;
            margin: 0;
            color: rgba(255, 255, 255, 0.95);
            letter-spacing: -4px;
            text-shadow: 0 0 50px rgba(0, 255, 136, 0.3);
            animation: breathe 5s ease-in-out infinite;
        }

        @keyframes breathe {
            0%, 100% { opacity: 0.9; }
            50% { opacity: 1; }
        }

        .subtitle {
            font-size: 32px;
            color: rgba(255, 255, 255, 0.6);
            font-weight: 300;
            line-height: 1.4;
            max-width: 1000px;
            font-style: italic;
            margin: 0;
        }

        .cryptic-hints {
            display: flex;
            gap: 120px;
            margin-top: 0;
            opacity: 0.7;
            justify-content: center;
        }

        .hint {
            display: flex;
            align-items: center;
            gap: 20px;
            font-size: 24px;
            color: rgba(255, 255, 255, 0.5);
            font-family: 'Courier New', monospace;
        }

        .hint-icon {
            width: 32px;
            height: 32px;
            background: rgba(0, 255, 136, 0.2);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            animation: flicker 2s ease-in-out infinite;
        }

        .hint:nth-child(2) .hint-icon { animation-delay: 0.5s; }
        .hint:nth-child(3) .hint-icon { animation-delay: 1s; }

        .bottom {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .github-link {
            display: flex;
            align-items: center;
            gap: 20px;
            font-size: 28px;
            color: #00ff88;
            text-decoration: none;
            font-weight: 500;
        }

        .github-icon {
            width: 40px;
            height: 40px;
            fill: currentColor;
        }

        .watch-button {
            background: rgba(0, 255, 136, 0.1);
            border: 1px solid rgba(0, 255, 136, 0.3);
            color: rgba(0, 255, 136, 0.9);
            padding: 24px 48px;
            border-radius: 12px;
            font-size: 24px;
            font-weight: 400;
            box-shadow: 0 0 30px rgba(0, 255, 136, 0.1);
            animation: pulse-button 3s ease-in-out infinite;
        }

        @keyframes pulse-button {
            0%, 100% { box-shadow: 0 0 20px rgba(0, 255, 136, 0.1); }
            50% { box-shadow: 0 0 30px rgba(0, 255, 136, 0.2); }
        }

        /* Glow effects */
        .title {
            filter: drop-shadow(0 0 20px rgba(0, 255, 136, 0.3));
        }

        .logo img {
            filter: drop-shadow(0 0 20px rgba(0, 255, 136, 0.4));
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">
                <img src={nimbleToolsLogo.src} alt="NimbleTools Logo" width="80" height="80" />
            </div>
            <div class="brand">NimbleTools</div>
        </div>

        <div class="main-content">
            <div class="announcement-badge">
                <span class="eye">👁️</span>
                Something Stirs
            </div>

            <h1 class="title">It's Coming...</h1>
            
            <p class="subtitle">
                The foundation that powers the impossible.<br>
                What the community has been waiting for.
            </p>

            <div class="cryptic-hints">
                <div class="hint">
                    <div class="hint-icon">⚡</div>
                    <span>Instant Scale</span>
                </div>
                <div class="hint">
                    <div class="hint-icon">🔮</div>
                    <span>Enterprise DNA</span>
                </div>
                <div class="hint">
                    <div class="hint-icon">🚀</div>
                    <span>Zero Friction</span>
                </div>
            </div>
        </div>

        <div class="bottom">
            <a href="#" class="github-link">
                <svg class="github-icon" viewBox="0 0 24 24">
                    <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                </svg>
                github.com/NimbleBrainInc/nimbletools-core
            </a>
            
            <div class="watch-button">Keep Watching</div>
        </div>
    </div>
</body>
</html>